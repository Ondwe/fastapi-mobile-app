name = "fastapi-mobile-app"
main = "worker.py"
compatibility_date = "2024-11-05"
compatibility_flags = ["nodejs_compat", "python_workers"]

[env.production]
zone_id = "your-zone-id"
account_id = "your-account-id"

[vars]
ENVIRONMENT = "production"

# Remove [assets] section - we're building an API, not static site
# Remove [build] section - Cloudflare Pages handles this separately
